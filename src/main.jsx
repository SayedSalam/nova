import React from "react";
import ReactDOM from "react-dom/client";
import NovaCommerce from "./NovaCommerce.jsx";

document.body.style.margin = "0";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NovaCommerce />
  </React.StrictMode>
);
